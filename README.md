# qlora-mlops-pipeline

**An Automated MLOps Pipeline for Building Datasets and Fine-Tuning
Domain-Specialized LLMs for Task-Oriented Voice Agents** (es-MX).

This repository is the **pipeline**: corpus construction, QLoRA fine-tuning,
orchestration, versioning and evaluation. `src/agent/` is the reference runtime
that consumes the resulting adapter — it exists so the model can be evaluated
and demonstrated end to end, not as the product.

Two conversational flows, in Mexican Spanish, over the phone:

1. **Contact data validation** — confirm and correct name, phone and email, field by field.
2. **Appointment confirmation** — call 30 minutes before an appointment; reschedule if needed.

---

## Data provenance statement

This project uses **no proprietary data, code or prompts from any employer or client.**
The training corpus is built exclusively from:

- Public, license-verified corpora (SGD, Taskmaster, MultiWOZ, MASSIVE, CallCenterEN,
  CALLHOME Spanish, CIEMPIESS, TEDx Spanish)
- Synthetic dialogues generated by this pipeline

No recordings, transcripts or personal data of real individuals are used at any stage.
See `docs/DATA_PROVENANCE.md` for the per-source license audit.

## Language convention

| Scope | Language |
|---|---|
| Code, DAG ids, table and column names, graph nodes, JSON schemas, branches, commits, logs | **English** |
| Training corpus, prompts, model outputs, agent speech | **Spanish (es-MX)** |
| Project documentation and academic reports | **Spanish** |

No exceptions. Enforced in code review.

## Quick start

```bash
cp .env.example .env          # fill in the API keys
make up                       # airflow, spark, hdfs, kafka, postgres, mlflow, metabase
make init                     # DDL, MinIO buckets, Kafka topics
make test                     # unit tests, no infra required
```

| Service | URL |
|---|---|
| Airflow | http://localhost:8080 |
| MLflow | http://localhost:5000 |
| MinIO console | http://localhost:9001 |
| Metabase | http://localhost:3000 |
| Spark master | http://localhost:8081 |

## Pipeline

```
make ingest      # ingest_corpus            → bronze
make curate      # curate_spark             → silver
make localize    # translate_localize       → silver (es-MX)
make generate    # generate_combinatorial   → synthetic dialogues
make augment     # augment_asr_noise        → augmented pool
make gold        # build_gold_dataset       → gold + dvc push + tag
make train       # train_qlora              → MLflow run
make eval        # evaluate_model           → metrics vs frozen eval set
```

## Repository layout

- `dags/` — orchestration only. Every task calls a function in `src/`; **no logic here.**
- `src/pipeline/` — data construction and training. Needs Spark and, for training, a GPU.
- `src/agent/` — runtime. Latency-sensitive, no Spark, no GPU.
- `prompts/` — versioned artifacts. A prompt change is a new experiment.
- `schemas/` — JSON Schema for every model output. Invalid output is treated as `ambiguous`.
- `evaluation/eval_set/` — **frozen**. Do not edit after week 3.

## Reproducing any result

```bash
git checkout <tag>
dvc pull
make eval
```

## License

Code: Apache-2.0. Dataset and adapter are published separately — see `docs/PUBLISHING.md`.
